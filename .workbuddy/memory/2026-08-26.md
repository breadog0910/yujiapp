# 2026-08-26

## yuji-app 自我照顾模块交互改造
- 需求：保留「一行一行表单」外观，但交互含义升级为：直接打勾完成（去掉确认按钮）、点任务打开设置（每天一次/一直出现、不置顶/今日置顶/一直置顶、删除）、左滑露出跳过/置顶/设置、循环任务完成后沉到最底。
- state.js：careOptions 扩展字段 mode(daily/recurring)、pinned(false/today/always)、skipped、reward；ensureDaily 每日重置并清空「今日置顶」。
- tab1.js：renderCareOptions 改为行式卡片（保留 .care-card 滑动结构）；completeCare 直接完成+重置循环任务并沉底；新增 careConfig 弹窗的调用（点击行/设置按钮）。
- popups.js：补全 builders.careConfig（分段设置 UI）与 inits.careConfig（保存/删除逻辑）。
- CSS：tab1-home.css 重写 .care-card* 行式样式（原本 .care-row 旧样式已替换），popups.css 加 .cfg-* 分段控件与 .popup-btn.danger。
- 预览：python http.server 8123（yuji-app 目录）。
