# 2026-08-29

## Supabase 迁移 SQL 整理
- 现状：`supabase/migrations/` 原有 001-006 + 999_all_in_one(整合版) + 中途出现的 007_farm_multi_plot，双源且 tab3 背景三处不一致（002→tab3-garden-bg.jpg / 003→farm/land.png / 999→farm/tab3-bg.png）。
- 决策（用户让我定）：以 `999_all_in_one.sql` 为唯一真源。
- 操作：
  - 001-006 用 `git mv` 归档到 `supabase/migrations/_legacy/`（保留历史，CLI 不再应用）。
  - 把 007 的 `crop_key`/`sort_order` 列 + land-2/land-3 演示地块**并入 999**（否则 007 字母序排在 999 前，`supabase db push` 会先跑 007、因表未建而报错）。007 未纳入 git，用 `mv` 归档到 `_legacy/`。
  - tab3 背景按用户指定改为 `assets/tab-bg/tab3-1787835591477.png`（文件位于 yuji-app/assets/tab-bg/）。
  - 修正 `deploy.ps1`「下一步手动操作」：原来只提 001/002（漏农场迁移），改为指向 `999_all_in_one.sql` 并说明 `supabase db push` 方式。
  - 修正 docs/superpowers 下两份设计文档对 001/003 的失效路径引用 → `_legacy/`。
- 结论：`migrations/` 顶层现仅 `999_all_in_one.sql`（单事务，幂等，整合 001-007）。部署只需 `supabase db push` 或 SQL Editor 跑 999。
- 修复 yuji-app/admin/admin.js 家具删除报错 "RESOURCE_CONFIG is not defined"：第415行误写变量名，改为局部配置对象 resources['room-layout']。

## 技能农场（Tab3）单地块 → 多地块改造
- 需求：后台可复制多块土地、可拖拽摆放位置、可保存、每块独立设置作物；Tab3 页面去掉像素小人（garden-character）。
- DB：crop_key/sort_order 列已在 999_all_in_one.sql 的 farm_land_config 中（建表+幂等 ALTER+演示 land-2/land-3 种子），RLS 表级策略自动覆盖多行。
- admin/admin.js：farm-land 资源绑定 farm_land_config（mapBack 写 crop_key/sort_order）；新增 farmLands 数组 + FARM_LAND_DEFAULTS/newFarmLandId/normalizeFarmLand；renderFarmStage 按 z 排序渲染全部土地、renderFarmLandList 列表、bindFarmLandEvents 每块拖拽、#flp-crop 每块作物下拉；PUT 走「删全部+批量 insert」数组替换（377 行分支）。
- api.js getConfig：farm_land_config 改为 .order('sort_order') 取全量数组，返回 farmLandList（保留 farmLandConfig=首行）。
- state.js：新增 farmLandList 状态 + getter；applyConfig 优先用数组覆盖（空则回退 farmLandConfig）；fingerprint L 用 farmLandList。
- tab3.js：renderLands() 遍历 State.farmLandList（回退 [farmLandConfig]）逐块渲染 .farm-land，含后端配置的作物阶段 overlay；index.html 单图 farmLandLayer 换成 #farmLandsLayer 容器；tab3-garden.css 移除 .garden-character 整块，index.html 移除小人 DOM。
- _test_index.html 旧测试快照同步移除 garden-character 块（注释标注已按需求移除），保持与正式页面一致。
- 验证：4 个 JS 文件 node --check 无语法错误；grep 无 farmLandLayer/garden-character/FARM_LAND_ID 残留引用（仅 tab3.js 注释）。
