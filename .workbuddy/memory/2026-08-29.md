# 2026-08-29 工作日志

## Tab3 技能农场完善

### 1. 生成像素风作物图片（12张）
- 小麦 4 阶段：`wheat-s1.png` ~ `wheat-s4.png`（破土→生长→繁茂→成熟）
- 向日葵 4 阶段：`sunflower-s1.png` ~ `sunflower-s4.png`
- 幻菇 4 阶段：`mushroom-s1.png` ~ `mushroom-s4.png`
- 全部透明背景、像素风、统一风格
- 存放路径：`yuji-app/assets/farm/crops/`

### 2. 完善前端代码
- `state.js`：更新 DEFAULT_FARM_CROP_CATALOG，使用新生成的作物图片
- `tab3-garden.css`：
  - 增强土地 hover/点击交互反馈（缩放+光晕）
  - 添加空土地呼吸光晕提示（empty-land）
  - 作物入场动画（crop-pop-in）
  - 作物风吹摇摆动画（crop-sway）
  - 作物成熟发光脉冲动画（crop-mature + crop-glow）
  - 引导提示浮动动画（hint-bob）
  - 新增萤火虫粒子效果（firefly）
- `tab3.js`：
  - 渲染作物时根据阶段自动添加 CSS 动画类
  - 空土地添加 empty-land 类显示呼吸光晕
  - 添加萤火虫粒子（白天2个/晚上6个）
  - 修正 ASSET_FALLBACK 路径

### 3. Ardot 设计稿
- 创建了「予己-tab3-技能农场设计」设计文件
- 包含：Tab3 主界面、种植弹窗、农场日志弹窗、后台作物管理界面

### 4. 后台管理
- admin 后台已有完整的作物品种管理（新增/编辑/删除/阶段图上传）
- 土地可独立设置作物，无需额外修改

## 自我说明书（selfManual）功能排查
- 入口：Tab1 点「镜子」家具 → 🪞 自我洞察说明书弹窗（popups.js selfManual()）
- 生成方式：手动按钮「✨让AI重新总结我」触发，**不自动**；五章默认占位「还在认识中…」
- 后端链路完整：`POST /api/ai/chain` {chain:'insight_manual'} → insight 分析 → self_manual 写回 `user_state.data.selfManual`（saveToSelfManual）
- 当前状态：`ai_config` 中 self_manual / insight 默认 `enabled=0` 且 `api_key` 为空；前端据此隐藏「重新总结」按钮、改显「未配置 AI 智能体」提示，故内容始终为占位
- 用户要求：入口/弹窗处不要出现「未配置 / 啥没配置」类提示文案（疑似指 aiDisabledHint，待用户确认是否即此）

## 全局文案替换：关爱值 → 幸福值
- 需求：把项目中所有「关爱值」改为「幸福值」（仅显示文案/注释/文档，**不动英文字段名 careValue**）
- 已修改 10 个文件：`prd3.3.md`、`server/src/context.js`、`supabase/functions/_shared/context.ts`、`yuji-app/{index.html,_test_index.html,css/tab1-home.css,js/tab1.js,js/popups.js}`、`generated-images/shots/{dom-edit.txt,dom-normal.txt}`
- 未处理：`generated-images/shots/*/Cache_Data/f_*`（Chromium 截图二进制缓存，非源码）
- 注意：项目已有独立的「开心值 happinessValue」字段，幸福值仍指向 careValue，二者并存未合并
- 已推送：`6875755` feat(yuji-app): 关爱值统一改名为幸福值（显示文案/注释/PRD，字段 careValue 不变），5 文件 42+/42-，已 push 到 origin/main。
- 提交注意：`popups.js` 中混有并行的「数据隐私」功能代码（privacy 弹窗），首次提交误打包，已拆分：`6875755` 只含幸福值替换；`6568b3d` 补回 privacy 函数 127 行。期间另有并行提交 `78dd719`（数据隐私开关，含 state.js/account.js/popups.css/index.html v76）与 `c0ddd27`（docs 工作记忆）。当前 HEAD=远程 main=`6568b3d`。

## Tab1 前后台家具摆放对齐修复（反复出现的老问题）
- 现象：Tab1「此刻·家」房间，前台 App 渲染的家具位置与后台编辑/预览摆放数据不对应（同一套 x%/y% 在不同屏幕错位）。
- 根因：家具用「px 固定尺寸 + 百分比定位」混合。前台 .room-furniture 家具层偏移写死 `--care-h:124px`，后台画布(390×844)用 `bottom:14.69%`(=124/844)。两者只在房间恰好 844px 高时相等；前台手机框按 390/844 等比缩放，高度几乎不是 844px，于是两端家具层高度不同 → 同一 x%/y% 绝对位置错位。
- 修复：前台改为与后台同一比例 `--care-h:14.69%`（CSS 默认 + tab1.js 显式写值两处统一）。两端家具层高度都 = 舞台高度×85.31%，任意屏幕尺寸 WYSIWYG。
- 关键约束：前台气泡条是固定像素高度(≈124px)，在矮于 844 的屏上家具层底边会略低于气泡条顶(≤~10px)，属可忽略视觉留白/微叠，不破坏摆放对应。
- 提交：`1606b8a` fix(tab1): 家具层偏移改用 14.69% 与后台画布等比对齐；已 push 到 origin/main。
- 关联坑：上次修复(499a26f)是把 px 固定为 124 试图对齐，但本质是脆弱方案；本次改为等比百分比才是根治。

## 修复：main.js 双击底部导航栏误触"重置所有数据"弹窗
- 现象：用户反馈"随便点哪里有时候会跳出重置"。根因是 `yuji-app/js/main.js` 里有段"调试用"代码：把 `dblclick` 监听绑在底部 `.tab-bar`（整条导航栏），触发即 `confirm('确定要重置所有数据吗？')` → `State.reset() + reload()`。手机/触摸板极易误触，且误点确定会清空全部本地数据，风险高。
- 附带 bug：`bootApp()` 被调用两次（77 行与 86 行），导致所有事件（含该重置监听）被绑定两份。
- 修复：删除该调试监听块；删除 86 行多余的 `bootApp()` 重复调用，现每路径仅初始化一次。
- 提交：`3829a70` fix(yuji-app): 移除调试残留的双击重置监听，避免误触清空数据；已 push 到 origin/main。
- 教训：调试/重置类能力不要绑在任意区域双击这种易误触方式；如确需重置入口，应做成明确按钮 + 二次确认。

## 数据隐私功能完善（本次收尾）
- 用户要求：数据隐私逻辑写清楚 + 有单独选项 + 设置里有单独「数据隐私」按钮。
- 实现（已与早先 78dd719 / 6568b3d 合并，本次核对完整性并修 bug）：
  - `state.js`：`PRIVACY_DEFAULTS`（localOnly=true / allowAiRead=false / allowAnalytics=true，偏向隐私）+ 注释写清四条原则；`buildDefaultState().privacy`；`shouldSyncToCloud()` 让 `scheduleSync` 尊重 localOnly（本地优先不上传云端）；`aiEnabled` 增加 `allowAiRead` 闸门（未授权则禁用信件/说明书/家具故事/树洞 AI）；新增 `aiReadAllowed/analyticsAllowed/getPrivacy/setPrivacy/track`。
  - `account.js`：齿轮下拉菜单加「🛡 数据隐私」按钮（`.go.ghost` 样式）→ `Popups.open('privacy')`。
  - `popups.js`：`privacy` 弹窗——清楚列出「数据隐私逻辑」四条 + 三个独立开关（本地存储优先 / 允许 AI 读取 / 允许匿名统计）+ 导出本机 JSON / 清除本机数据（二次确认）。Tab2 本心对语 whisper 也加了 `aiReadAllowed` 闸门。
  - `popups.css`：`.privacy-row / .privacy-switch` 开关样式；`index.html`、`_test_index.html` 资源升 `?v=76`。
- 修的 bug：`setPrivacy` 里 `wasLocalOnly` 原在 `Object.assign` 之后读取（永远等于新值），导致「本地优先→允许云端」时不会触发上传；改为先读旧值再应用。
- 校验：三 JS `node --check` 通过；无重复 `privacy()` 定义。
- 注意：设置齿轮仅在 Tab1 显示（已有逻辑），未登录本地玩家看不到该按钮；其数据默认本机私有，无需额外设置。

## 镜子弹窗缺少 AI 授权入口（bug 修复）
- 现象：用户反馈"镜子的隐私设置没办法进行授权操作"。
- 根因：`selfManual()`（🪞 自我洞察说明书）init 里 AI 可用判断只检查后台 `aiEnabled('insight'|'self_manual')`，完全没有用户侧「允许 AI 读取」维度；弹窗既无授权开关、也无引导去授权的入口，未授权时用户无处可点。
- 修复（`popups.js`）：
  - builder 增加 `#aiAuthHint` 提示区，含「🔓 授权 AI 读取我的记录」按钮（`data-act="grantAi"`）；
  - init 增加 `authed = !(State.aiReadAllowed && !State.aiReadAllowed())`，三态互斥显示：后台未开智能体→`aiDisabledHint`；已开但用户未授权→`aiAuthHint`；都 OK→`regen`（让 AI 重新总结我）按钮；
  - `grantAi` 点击直接 `State.setPrivacy({allowAiRead:true})` + toast，然后重开 selfManual；regen 绑定条件补 `&& authed`。
- 校验：`node --check` 通过；`grantAi`/`aiAuthHint`/`aiReadAllowed` 引用一致。
- 提交：`28cb47b` fix(mirror): …；资源升 `?v=78`（_test_index.html 本地 v23，gitignored 不进仓库）。

## 同机切换账号家具位置串号（bug 修复）
- 现象：用户反馈"之前建的账户的家具位置，第二次换一个账户登录时会保留上一次账户的家具位置"——同机（同浏览器 localStorage）切账号数据不切换。
- 根因：`state.js` 的 `STORAGE_KEY='yuji_state_v5'` 是写死的单一 key，`load()/save()` 都读它，不按账号区分；`State.init()` 的 authed 分支也只是 `load()`，没有按账号取数据（原设计注释说"按登录账号从 Api.getState 读取"，但已退化成纯本地单一 key）。
- 修复（`state.js`）：
  - 新增 `storageKey()`：已登录→`yuji_state_v5_<userId>`，未登录→共享 `yuji_state_v5`（游客）；`load()/save()` 改用之，实现按账号隔离、仍保持"只存本机"的隐私设计。
  - 新增 `migrateLegacyIfNeeded()`：登录账号首次加载时，若账号专属 key 为空且旧共享 key 有数据，把旧数据搬到账号专属 key 并删除旧 key（只迁移一次），避免老用户升级后家具"消失"。
  - `State.init()` authed 分支先 `migrateLegacyIfNeeded()` 再 `load()`。
- 校验：`node --check` 通过；`STORAGE_KEY` 仅作 base/guest key 使用，无其它写死写入。
- 提交：`0461599` fix(state): …；资源升 `?v=79`（_test_index.html 本地 v24）。
- 时序：main.js 中 `Api.init()` 先于 `State.init()` 执行，故切换账号时 `Api.getUser().id` 已就绪。

## 自我说明书「后端生成了但不显示」bug 修复
- 现象：点「让 AI 重新总结我」后弹"说明书已在后端生成，3 秒后自动刷新～"，但重开后仍是占位"还在认识中…"。
- 根因（popups.js 原 2035-2038 的 else 死胡同）：前端取数顺序 ①正则解析 `self_manual` 那步散文文本（基本解析不出 JSON）→ `newManual` 为 null；②退路 `Api.getState()` 仅 `if(Api.isAuthed())` 才执行（未登录/预览/本地未登录被跳过）；③后端 `saveToSelfManual` 在 DeepSeek 调用 `!r.ok` 时 `return` 提前退出，**说明书根本没写库**，而前端仍谎称"已在后端生成"。else 分支从不写 `State.state.selfManual`，重开即读旧占位。
- 修复：
  - popups.js：取数改为 ①优先读后端响应里新增的 `result.manual` 字段 → ②正则解析 steps → ③`Api.getState()` 带 3 次重试（不再强制登录态）；取到就写本地+显示，取不到弹**真实错误**（"小我这次没能把说明书保存下来…"）而非谎称成功。
  - supabase/functions/ai-chain/index.ts 与 server/src/routes/ai.js：链返回里新增 `manual: data.selfManual`（已落库那份），前端一次拿到，免去二次拉取/登录态依赖。
- 生效条件：仍须 ①用户已登录（forced login）②后台 ai_config 的 insight+self_manual 已 enabled 且 DeepSeek key 有效（否则 `saveToSelfManual` 写库失败，前端现在会诚实报错）。云端改动需 `supabase functions deploy ai-chain` 才生效；本地 server 重启即生效。
- 状态：代码已改（未提交），等用户确认是否 commit/push。
