# 2026-08-29

## Supabase 迁移 SQL 整理
- 现状：`supabase/migrations/` 原有 001-006 + 999_all_in_one(整合版) + 中途出现的 007_farm_multi_plot，双源且 tab3 背景三处不一致（002→tab3-garden-bg.jpg / 003→farm/land.png / 999→farm/tab3-bg.png）。
- 决策（用户让我定）：以 `999_all_in_one.sql` 为唯一真源。
- 操作：
  - 001-006 用 `git mv` 归档到 `supabase/migrations/_legacy/`（保留历史，CLI 不再应用）。
  - 把 007 的 `crop_key`/`sort_order` 列 + land-2/land-3 演示地块**并入 999**（否则 007 字母序排在 999 前，`supabase db push` 会先跑 007、因表未建而报错）。007 未纳入 git，用 `mv` 归档到 `_legacy/`。
  - tab3 背景按用户指定改为 `assets/tab-bg/tab3-1787835591477.png`（文件位于 yuji-app/assets/tab-bg/）。
  - 修正 `deploy.ps1`「下一步手动操作」：原来只提 001/002（漏农场迁移），改为指向 `999_all_in_one.sql` 并说明 `supabase db push` 方式。
  - 修正 docs/superpowers 下两份设计文档对 001/003 的失效路径引用 → `_legacy/`。
- 结论：`migrations/` 顶层现仅 `999_all_in_one.sql`（单事务，幂等，整合 001-007）。部署只需 `supabase db push` 或 SQL Editor 跑 999。
- 修复 yuji-app/admin/admin.js 家具删除报错 "RESOURCE_CONFIG is not defined"：第415行误写变量名，改为局部配置对象 resources['room-layout']。
